# Feature flags

> **Status:** stub. Part of the initial Akita Book scaffold.

Document `parallel` (Rayon, default), `disk-persistence`, `zk`, and
`logging-transcript`: what each enables, default-on vs opt-in, and the explicit
"no backward-compatibility guarantee" warning for integrators.

## Sources to fold in

- `AGENTS.md` (Feature Flags)
- `crates/akita-pcs/Cargo.toml`, per-crate `Cargo.toml` feature tables
