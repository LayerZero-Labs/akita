[package]
name = "akita-pcs"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"
authors = [
    "Markos Georghiades <mgeorghiades@a16z.com>",
    "Quang Dao <qvd@andrew.cmu.edu>",
    "Amirhossein Khajehpour <amir.kh@layerzerolabs.org>",
    "Omid Bodaghi <omibo@layerzero.foundation>",
    "Giuseppe Vitto <giuseppe.vitto@layerzerolabs.org>",
]
license = "Apache-2.0 OR MIT"
description = "A high performance and modular implementation of the Akita polynomial commitment scheme, descended from Hachi."
repository = "https://github.com/LayerZero-Labs/hachi"
documentation = "https://docs.rs/akita-pcs"
readme = "../../README.md"
keywords = ["cryptography", "zkp", "commitment", "polynomial", "pcs", "lattice", "post-quantum"]
categories = ["cryptography", "algorithms"]
include = [
    "src/**",
    "examples/**",
    "benches/**",
    "Cargo.toml",
]

[package.metadata.docs.rs]
all-features = true

[features]
default = ["parallel", "planner"]
parallel = [
    "dep:rayon",
    "akita-algebra/parallel",
    "akita-config/parallel",
    "akita-field/parallel",
    "akita-prover/parallel",
    "akita-scheme/parallel",
    "akita-setup/parallel",
    "akita-verifier/parallel",
]
disk-persistence = ["akita-setup/disk-persistence"]
planner = [
    "akita-config/planner",
    "akita-scheme/planner",
    "akita-setup/planner",
]
# Forwards `akita-field/op-counter`. Enables the verifier
# field-multiplication counter for measurement runs (book §5.8 lines
# 1104–1126). Production builds must NOT enable this feature; it
# pays one atomic branch per `Fp::mul` call. See
# `akita_field::op_counter` for the API contract.
op-counter = ["akita-field/op-counter"]

[dependencies]
thiserror = "2.0"
rand_core = { version = "0.6", features = ["getrandom"] }
akita-algebra = { version = "0.1.0", path = "../akita-algebra" }
akita-challenges = { version = "0.1.0", path = "../akita-challenges" }
akita-config = { version = "0.1.0", path = "../akita-config" }
akita-field = { version = "0.1.0", path = "../akita-field" }
akita-serialization = { version = "0.1.0", path = "../akita-serialization" }
akita-sumcheck = { version = "0.1.0", path = "../akita-sumcheck" }
akita-transcript = { version = "0.1.0", path = "../akita-transcript" }
akita-types = { version = "0.1.0", path = "../akita-types" }
akita-verifier = { version = "0.1.0", path = "../akita-verifier" }
akita-prover = { version = "0.1.0", path = "../akita-prover" }
akita-scheme = { version = "0.1.0", path = "../akita-scheme" }
akita-setup = { version = "0.1.0", path = "../akita-setup" }
tracing = "0.1"
rayon = { version = "1.10", optional = true }
blake2 = "0.10.6"

[dev-dependencies]
akita-planner = { version = "0.1.0", path = "../akita-planner" }
akita-sumcheck = { version = "0.1.0", path = "../akita-sumcheck", features = ["test-helpers"] }
akita-verifier = { version = "0.1.0", path = "../akita-verifier", features = ["test-helpers"] }
rand = "0.8"
criterion = { version = "0.5", features = ["html_reports"] }
num-bigint = "0.4.6"
ark-bn254 = { git = "https://github.com/a16z/arkworks-algebra", branch = "dev/twist-shout", features = ["scalar_field"] }
ark-ff = { git = "https://github.com/a16z/arkworks-algebra", branch = "dev/twist-shout" }
tracing-chrome = "0.7"
tracing-subscriber = { version = "0.3", features = ["env-filter", "registry"] }

[[example]]
name = "profile"

[[bench]]
name = "ring_ntt"
harness = false

[[bench]]
name = "field_arith"
harness = false

[[bench]]
name = "fp64_reduce_probe"
harness = false

[[bench]]
name = "akita_e2e"
harness = false

[[bench]]
name = "onehot_batched_commit"
harness = false

[[bench]]
name = "onehot_batched_opening"
harness = false

[[bench]]
name = "root_kernels"
harness = false

[[bench]]
name = "fft_smooth"
harness = false

[lints.rust]
missing_docs = "warn"
unreachable_pub = "warn"

[lints.clippy]
missing_errors_doc = "warn"
missing_panics_doc = "warn"
