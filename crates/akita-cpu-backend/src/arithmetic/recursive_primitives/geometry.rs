//! Canonical recursive relation geometry.
//!
//! Geometry authorities currently live in `akita-types` so prover and verifier
//! share one definition. Backend implementations consume those authorities
//! directly instead of duplicating them here.
