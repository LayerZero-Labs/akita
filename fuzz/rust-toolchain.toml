# cargo-fuzz needs nightly sanitizer flags. Pin the date so campaign builds
# are reproducible; bump deliberately together with Cargo.lock.
[toolchain]
channel = "nightly-2026-09-01"
profile = "minimal"
components = ["llvm-tools-preview", "rustfmt"]
